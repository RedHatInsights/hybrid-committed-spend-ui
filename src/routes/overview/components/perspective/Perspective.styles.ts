import React from 'react';

export const styles = {
  perspective: {
    display: 'flex',
    alignItems: 'center',
  },
  perspectiveContainer: {
    display: 'flex',
  },
} as { [className: string]: React.CSSProperties };
